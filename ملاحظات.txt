corn يبحث كل دقيقة المفروض كل 5 دقائق
-------------------
نقطة ضعف بسيطة:

بعض الـ enums (status / roles) ما زالت Strings حرة

ليست مشكلة الآن، لكن مستقبلًا يُفضّل توحيدها
--------------------
WebSocket v1

حدث واحد فقط: bid:new

polling fallback

بدون Auth socket

بدون تقليل polling عدواني

بدون Redis adapter