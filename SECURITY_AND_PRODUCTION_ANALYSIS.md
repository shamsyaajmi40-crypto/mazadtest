# تحليل أمني وجاهزية الإنتاج — منصة Mazad

تم إعداد هذا التقرير بناءً على مراجعة الكود (Backend + Frontend) للمشروع.

---

## 1. نظرة عامة على المشروع

| الجانب | الوصف |
|--------|--------|
| **الواجهة** | Vite + React 19 + TypeScript + Tailwind + Framer Motion |
| **الخادم** | Express.js + MongoDB + Socket.io |
| **الوظائف** | مزادات، مصادقة، مدفوعات ZainCash، محفظة، إشعارات، إدارة، شحن |

---

## 2. نقاط القوة (ما تم تنفيذه جيداً)

- **مصادقة وحماية مسارات**: استخدام JWT مع `protect` و `requireRole` على المسارات الحساسة.
- **منع حقن NoSQL**: استخدام `mongoSanitize` على `body`, `query`, `params`.
- **تأمين الملفات**: Multer مع تحديد MIME (jpeg, png, webp) وحجم ملف، أسماء ملفات عشوائية.
- **تحديد معدل الطلبات**: Rate limit على تسجيل الدخول والمزايدة و OTP.
- **تخفيف XSS**: استخدام مكتبة `xss` و `validateText` في التحقق من النصوص.
- **كلمات مرور**: تشفير بكثافة 10 مع bcrypt.
- **قواعد أمان HTTP**: استخدام Helmet.
- **CORS**: قائمة منشآت مسموحة (لا يوجد `*`).
- **منع المزايدة على النفس**: في `placeBid` يتم رفض مزايدة البائع على مزاده.
- **معاملات قاعدة البيانات**: استخدام sessions و transactions في عمليات مالية (مثل placeBid، ZainCash).
- **تحقق من المدخلات**: دوال تحقق للنصوص والأرقام والتواريخ وأرقام الهواتف العراقية.

---

## 3. ثغرات ومخاطر أمنية

### حرجة (يجب معالجتها قبل الإنتاج)

#### 3.1 تسريب بيانات اتصال قاعدة البيانات

- **الملف**: `backend/db-check.js`
- **المشكلة**: وجود سلسلة اتصال MongoDB مكتوبة داخل الكود وتحتوي على اسم مستخدم وكلمة مرور.
- **الخطر**: من يصل إلى المستودع (أو نسخة من الكود) يمكنه الوصول إلى قاعدة البيانات بالكامل.
- **الإجراء المقترح**:
  - حذف أي سلسلة اتصال من الكود.
  - الاعتماد على `process.env.MONGO_URI` فقط (مثل باقي المشروع).
  - تغيير كلمة مرور قاعدة البيانات فوراً إذا كان هذا الملف قد تم رفعه أو مشاركته.

#### 3.2 تسريب معلومات حساسة في السجلات (Logs)

- **الملف**: `backend/server.js` (حوالي السطر 214)
- **المشكلة**: `console.log("MONGO_URI =", process.env.MONGO_URI)` يطبع عنوان الاتصال بقاعدة البيانات (وقد يتضمن كلمة المرور) في السجلات.
- **الخطر**: في بيئة الإنتاج أو عند مشاركة الـ logs يظهر سر الاتصال.
- **الإجراء المقترح**: إزالة طباعة `MONGO_URI` في الإنتاج، أو طباعة شيء مثل `"MONGO_URI is set"` فقط.

#### 3.3 مفتاح سري افتراضي ضعيف

- **الملف**: `backend/utils/receipt.js` (حوالي السطر 22)
- **المشكلة**: `process.env.RECEIPT_SECRET || "mazad_integrity_secret_2024"` — في حال عدم تعيين المتغير يتم استخدام قيمة ثابتة ومعروفة.
- **الخطر**: تزوير أو التلاعب بتوقيع الإيصالات إذا عُرف المفتاح.
- **الإجراء المقترح**: عدم استخدام قيمة افتراضية؛ في الإنتاج إجبار وجود `RECEIPT_SECRET` قوي وإيقاف التطبيق إذا لم يُعيّن.

---

### متوسطة (يُنصح بمعالجتها)

#### 3.4 عدم التحقق من قوة كلمة المرور عند التسجيل

- **الملف**: `backend/controllers/auth.controller.js` — دالة `register`
- **المشكلة**: لا يوجد تحقق من طول أو تعقيد كلمة المرور (بينما `changePassword` تفرض على الأقل 6 أحرف).
- **الخطر**: سماح كلمات مرور ضعيفة جداً (مثل "1" أو "12345") يسهل التخمين والهجمات.
- **الإجراء المقترح**: فرض حد أدنى (مثلاً 8 أحرف) ويفضل أيضاً تعقيد (حروف، أرقام، رموز) في التسجيل وتغيير كلمة المرور.

#### 3.5 احتمال ReDoS من حقل البحث

- **الملف**: `backend/controllers/auction.controller.js` — دالة `getAuctions`
- **المشكلة**: `searchTerm` يُمرَّر مباشرة إلى `$regex` بدون تهريب أو تحديد طول.
- **الخطر**: إدخال أنماط regex معقدة قد يسبب استهلاكاً عالياً لوحدة المعالجة (ReDoS).
- **الإجراء المقترح**: تهريب رموز الـ regex في `searchTerm` أو استخدام بحث نصي آمن (مثلاً index)، وتحديد طول أقصى لسلسلة البحث.

#### 3.6 خطأ في صلاحيات مسارات الرصيد (Balance)

- **الملف**: `backend/routes/balance.routes.js` (حوالي السطر 22)
- **المشكلة**: `requireRole("superadmin", "admin")` — في `roles.js` الدالة تأخذ معاملاً واحداً، والقيمة في النموذج هي `"superAdmin"` (بحرف S كبير).
- **الخطر**: المستخدمون من نوع `admin` قد يُرفضون من مسارات مثل قائمة طلبات الرصيد رغم أن التصميم يقصد السماح لهم.
- **الإجراء المقترح**: توحيد استدعاء الصلاحيات مع نموذج المستخدم، مثلاً `requireRole("admin", "superAdmin")` مع التأكد أن `roles.js` يدعم عدة أدوار إن لزم.

#### 3.7 مسار ZainCash redirect بدون مصادقة

- **الملف**: `backend/controllers/zaincash.controller.js` — `zaincashRedirect`
- **الوضع**: المسار يعتمد على `transactionId` و `orderId` من الـ query ويطابقها مع سجلات الدفع. لا يوجد تحقق من توقيع الطلب من بوابة الدفع.
- **الملاحظة**: الخطر محدود إذا كانت البوابة لا ترسل المستخدم إلا بعد الدفع، لكن أي رابط يصل للمستخدم يمكن إعادة استخدامه.
- **الإجراء المقترح**: التأكد من أن البوابة تستخدم توقيع/توكن لا يمكن تزويره، وعدم الاعتماد فقط على معرفات يمكن تخمينها. مراجعة توثيق ZainCash للتوصيات.

---

### منخفضة / تحسينات

- **عدم وجود rate limit عام**: مسارات مثل إنشاء مزاد، طلبات الرصيد، إلخ لا يوجد عليها حد معدل طلبات عام؛ يفضل إضافة حد عام (مثلاً 100–200 طلب/دقيقة لكل IP) بالإضافة إلى الحدود الحالية.
- **تفاصيل الأخطاء في وضع التطوير**: في الإنتاج يتم إرجاع "Internal Server Error" فقط (جيد)، لكن التأكد من عدم تسريب الـ stack أو تفاصيل قاعدة البيانات في أي مسار.
- **صلاحية JWT**: انتهاء الصلاحية 30 يوم للتسجيل و 7 أيام لتسجيل الدخول معقول؛ يمكن تقليله قليلاً لزيادة الأمان إذا رغبت.

---

## 4. جاهزية الإنتاج (Production Readiness)

### ما هو جاهز

- فصل بيئة التطوير عن الإنتاج (مثلاً `NODE_ENV`).
- تعيين `secure: true` للكوكيز في الإنتاج.
- إخفاء تفاصيل الأخطاء في الإنتاج.
- استخدام متغيرات بيئة للأسرار والاتصالات (باستثناء الملفات المذكورة أعلاه).
- وجود معالجة أخطاء عامة (global error handler).
- استخدام `trust proxy` عند وجود reverse proxy.

### ما ينقص أو يُفضّل تحسينه

| البند | الوضع | التوصية |
|--------|--------|----------|
| **ملف .env.example** | غير موجود | إضافة `.env.example` يوضح المتغيرات المطلوبة دون قيم حقيقية |
| **التحقق من المتغيرات عند البدء** | غير موحد | التحقق عند التشغيل من وجود `JWT_SECRET`, `MONGO_URI`, `FRONTEND_URL` (وغيرها الحرجة) وإيقاف التطبيق إذا نَقصت |
| **سجلات (Logging)** | `console.log` فقط | استخدام مكتبة logging (مثل Pino/Winston) مع مستويات (info, error) وتجنب طباعة أسرار |
| **مراقبة الصحة (Health check)** | لا يوجد مسار صريح | إضافة مسار مثل `GET /health` يرجع 200 عند جاهزية التطبيق وقاعدة البيانات |
| **عرض تفاصيل المزاد بدون تسجيل دخول** | `GET /auctions/:id` يتطلب `protect` | إذا كانت المزادات عامة، يمكن جعل القراءة بدون مصادقة مع إخفاء بيانات حساسة (مثل بيانات البائع الكاملة) |
| **نسخ احتياطي وقاعدة بيانات** | غير واضح من الكود | التأكد من نسخ احتياطي منتظم لـ MongoDB وخطة استعادة |
| **HTTPS** | يعتمد على البيئة | التأكد من تشغيل الخادم خلف reverse proxy مع HTTPS في الإنتاج |
| **حد معدل طلبات عام** | غير موجود | إضافة rate limit عام لتقليل هجمات الرفض والاستخدام المفرط |

---

## 5. خلاصة وتوصيات مرتبة بالأولوية

### قبل رفع المنصة للإنتاج

1. **إزالة أو تصحيح `backend/db-check.js`**: عدم تخزين أي credentials داخل الكود، واستخدام `MONGO_URI` من البيئة فقط، وتغيير كلمة مرور قاعدة البيانات إذا كانت قد تعرضت.
2. **إيقاف طباعة `MONGO_URI`** (وأي أسرار أخرى) في السجلات.
3. **جعل `RECEIPT_SECRET` إجبارياً** في الإنتاج وبدون قيمة افتراضية ثابتة.
4. **إضافة تحقق من قوة كلمة المرور** في التسجيل (طول أدنى وتعقيد إن أمكن).
5. **معالجة استخدام `searchTerm` في البحث** (تهريب regex أو بحث آمن + حد أقصى للطول).
6. **تصحيح صلاحيات مسارات الرصيد** (`requireRole` وتمييز `superAdmin` vs `admin`).

### تحسينات إضافية للإنتاج

- إضافة `.env.example` والتحقق من المتغيرات عند البدء.
- إضافة rate limit عام.
- إضافة مسار health check.
- استبدال `console.log` بنظام logging مناسب وتجنب تسريب الأسرار.
- مراجعة توثيق ZainCash لتعزيز أمان مسار الـ redirect/callback.

---

*تم إعداد التقرير آلياً بناءً على مراجعة الكود. يُنصح بإجراء اختبار اختراق (penetration test) ومراجعة أمنية يدوية قبل الإطلاق الفعلي.*
